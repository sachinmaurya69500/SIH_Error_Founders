{
  "name": "ecoshield",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install --no-audit --no-fund && npm --prefix frontend run build",
    "dev:frontend": "npm --prefix frontend run dev",
    "preview:frontend": "npm --prefix frontend run preview"
  }
}
